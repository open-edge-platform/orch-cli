// Package catalog provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/deepmap/oapi-codegen version (devel) DO NOT EDIT.
package catalog

import (
	"encoding/json"
	"fmt"
	"time"
)

const (
	BearerAuthScopes = "BearerAuth.Scopes"
)

// Defines values for CatalogV3Kind.
const (
	KINDADDON       CatalogV3Kind = "KIND_ADDON"
	KINDEXTENSION   CatalogV3Kind = "KIND_EXTENSION"
	KINDNORMAL      CatalogV3Kind = "KIND_NORMAL"
	KINDUNSPECIFIED CatalogV3Kind = "KIND_UNSPECIFIED"
)

// APIExtensions represents some form of an extension to the external API provided by deployment package.
type CatalogV3APIExtension struct {
	// (OPTIONAL) Description of the API extension. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the API extension. When specified, it must be unique among all
	//  extensions of a given deployment package. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// One or more API endpoints provided by the API extension.
	Endpoints *[]CatalogV3Endpoint `json:"endpoints,omitempty"`

	// Name is a human-readable unique identifier for the API extension and must be unique for all extensions of a
	//  given deployment package.
	Name string `json:"name"`

	// UIExtension is an augmentation of an API extension.
	UiExtension *CatalogV3UIExtension `json:"uiExtension,omitempty"`

	// Version of the API extension.
	Version string `json:"version"`
}

// Application represents a Helm chart that can be deployed to one or more Kubernetes pods.
type CatalogV3Application struct {
	// Helm chart name.
	ChartName string `json:"chartName"`

	// Helm chart version.
	ChartVersion string `json:"chartVersion"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) Name of the profile to be used by default when deploying this application.
	//  If at least one profile is available, this field must be set.
	DefaultProfileName *string `json:"defaultProfileName,omitempty"`

	// (OPTIONAL) Description of the application. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the application. When specified, it must be unique among all
	//  applications within a project. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// ID of the project's registry where the Helm chart of the application is available for download.
	HelmRegistryName string `json:"helmRegistryName"`

	// (OPTIONAL) List of Kubernetes resources that must be ignored during the application deployment.
	IgnoredResources *[]CatalogV3ResourceReference `json:"ignoredResources,omitempty"`

	// (OPTIONAL) ID of the project's registry where the Docker image of the application is available for download.
	ImageRegistryName *string `json:"imageRegistryName,omitempty"`

	// Kind designation for applications and packages, normal (unspecified), extension, or addon.
	Kind *CatalogV3Kind `json:"kind,omitempty"`

	// Name is a human readable unique identifier for the application and must be unique for all applications of a
	//  given project. Used in network URIs.
	Name string `json:"name"`

	// Set of profiles that can be used when deploying the application.
	Profiles *[]CatalogV3Profile `json:"profiles,omitempty"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`

	// Version of the application. Used in combination with the name to identify a unique application within a project.
	Version string `json:"version"`
}

// ApplicationDependency represents the dependency of one application on another within the context of a deployment package.
//
//	This dependency is specified as the name of the application that has the dependency, and the name of the application
//	that is the dependency.
type CatalogV3ApplicationDependency struct {
	// Name of the application that has the dependency on the other.
	Name string `json:"name"`

	// Name of the application that is required by the other.
	Requires string `json:"requires"`
}

// ApplicationReference represents a reference to an application by its name and its version.
type CatalogV3ApplicationReference struct {
	// Name of the referenced application.
	Name string `json:"name"`

	// Version of the referenced application.
	Version string `json:"version"`
}

// Artifact represents a binary artifact that can be used for various purposes, e.g. icon or thumbnail for UI display, or
//
//	auxiliary artifacts for integration with various platform services such as Grafana dashboard and similar. An artifact may be
//	used by multiple deployment packages.
type CatalogV3Artifact struct {
	// Raw byte content of the artifact encoded as base64. The limits refer to the number of raw bytes.
	// bytes.const = []
	Artifact []byte `json:"artifact"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) Description of the artifact. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the artifact. When specified, it must be unique among all
	//  artifacts within a project. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// Artifact's MIME type. Only text/plain, application/json, application/yaml, image/png, and image/jpeg are allowed at this time.
	//
	//  MIME types are defined and standardized in IETF's RFC 6838.
	MimeType string `json:"mimeType"`

	// Name is a human-readable unique identifier for the artifact and must be unique for all artifacts within a project.
	Name string `json:"name"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`
}

// ArtifactReference serves as a reference to an artifact, together with the artifact's purpose within a deployment package.
type CatalogV3ArtifactReference struct {
	// Name of the artifact.
	Name string `json:"name"`

	// Purpose of the artifact, e.g. icon, thumbnail, Grafana dashboard, etc.
	Purpose string `json:"purpose"`
}

// Response message for the CreateApplication method.
type CatalogV3CreateApplicationResponse struct {
	// Application represents a Helm chart that can be deployed to one or more Kubernetes pods.
	Application CatalogV3Application `json:"application"`
}

// Response message for the CreateArtifact method.
type CatalogV3CreateArtifactResponse struct {
	// Artifact represents a binary artifact that can be used for various purposes, e.g. icon or thumbnail for UI display, or
	//  auxiliary artifacts for integration with various platform services such as Grafana dashboard and similar. An artifact may be
	//  used by multiple deployment packages.
	Artifact CatalogV3Artifact `json:"artifact"`
}

// Response message for the CreateDeploymentPackage method.
type CatalogV3CreateDeploymentPackageResponse struct {
	// DeploymentPackage represents a collection of applications (referenced by their name and a version) that are
	//  deployed together. The package can define one or more deployment profiles that specify the individual application
	//  profiles to be used when deploying each application. If applications need to be deployed in a particular order, the
	//  package can also define any startup dependencies between its constituent applications as a set of dependency graph edges.
	//
	//  The deployment package can also refer to a set of artifacts used for miscellaneous purposes,
	//  e.g. a thumbnail, icon, or a Grafana extension.
	DeploymentPackage CatalogV3DeploymentPackage `json:"deploymentPackage"`
}

// Response message for the CreateRegistry method.
type CatalogV3CreateRegistryResponse struct {
	// Registry represents a repository from which various artifacts, such as application Docker\* images or Helm\* charts
	//  can be retrieved. As such, the registry entity holds information used for finding and accessing the represented repository.
	Registry CatalogV3Registry `json:"registry"`
}

// DeploymentPackage represents a collection of applications (referenced by their name and a version) that are
//
//	deployed together. The package can define one or more deployment profiles that specify the individual application
//	profiles to be used when deploying each application. If applications need to be deployed in a particular order, the
//	package can also define any startup dependencies between its constituent applications as a set of dependency graph edges.
//
//	The deployment package can also refer to a set of artifacts used for miscellaneous purposes,
//	e.g. a thumbnail, icon, or a Grafana extension.
type CatalogV3DeploymentPackage struct {
	// (OPTIONAL) Optional set of application deployment dependencies, expressed as (name, requires) pairs of edges in the
	//  deployment order dependency graph.
	ApplicationDependencies *[]CatalogV3ApplicationDependency `json:"applicationDependencies,omitempty"`

	// List of applications comprising this deployment package. Expressed as (name, version) pairs.
	ApplicationReferences []CatalogV3ApplicationReference `json:"applicationReferences"`

	// Optional list of artifacts required for displaying or deploying this package. For example, icon or thumbnail
	//  artifacts can be used by the UI; Grafana\* dashboard definitions can be used by the deployment manager.
	Artifacts []CatalogV3ArtifactReference `json:"artifacts"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) Optional map of application-to-namespace bindings to be used as a default when deploying the applications that
	//  comprise the package.
	//  If a namespace is not defined in the set of "namespaces" in this Deployment Package,
	//  it will be inferred that it is a simple namespace with no predefined labels or annotations.
	DefaultNamespaces *CatalogV3DeploymentPackage_DefaultNamespaces `json:"defaultNamespaces,omitempty"`

	// (OPTIONAL) Name of the default deployment profile to be used by default when deploying this package.
	DefaultProfileName *string `json:"defaultProfileName,omitempty"`

	// (OPTIONAL) Description of the deployment package. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the deployment package. When specified, it must be unique among all
	//  packages within a project. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// Optional list of API and UI extensions.
	Extensions []CatalogV3APIExtension `json:"extensions"`

	// (OPTIONAL) Optional flag indicating whether multiple deployments of this package are forbidden within the same realm.
	ForbidsMultipleDeployments *bool `json:"forbidsMultipleDeployments,omitempty"`

	// (OPTIONAL) Flag indicating whether the deployment package has been deployed.
	//  The mutability of the deployment package entity can be limited when this flag is true. For example, one may
	//  not be able to update when an application is removed from a package after it has been marked as
	//  deployed.
	IsDeployed *bool `json:"isDeployed,omitempty"`

	// (OPTIONAL) Flag indicating whether the deployment package is visible in the UI.
	//  Some deployment packages can be classified as auxiliary platform extensions and therefore are to be deployed
	//  indirectly only when specified as deployment requirements, rather than directly by the platform operator.
	IsVisible *bool `json:"isVisible,omitempty"`

	// Kind designation for applications and packages, normal (unspecified), extension, or addon.
	Kind *CatalogV3Kind `json:"kind,omitempty"`

	// Name is a human-readable unique identifier for the deployment package and must be unique for all packages of a
	//  given project.
	Name string `json:"name"`

	// (OPTIONAL) Namespace definitions to be created before resources are deployed.
	//  This allows complex namespaces to be defined with predefined labels and annotations.
	//  If not defined, simple namespaces will be created as needed.
	Namespaces *[]CatalogV3Namespace `json:"namespaces,omitempty"`

	// Set of deployment profiles to choose from when deploying this package.
	Profiles *[]CatalogV3DeploymentProfile `json:"profiles,omitempty"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`

	// Version of the deployment package.
	Version string `json:"version"`
}

// (OPTIONAL) Optional map of application-to-namespace bindings to be used as a default when deploying the applications that
//
//	comprise the package.
//	If a namespace is not defined in the set of "namespaces" in this Deployment Package,
//	it will be inferred that it is a simple namespace with no predefined labels or annotations.
type CatalogV3DeploymentPackage_DefaultNamespaces struct {
	AdditionalProperties map[string]string `json:"-"`
}

// DeploymentProfile specifies which application profiles will be used for deployment of which applications.
type CatalogV3DeploymentProfile struct {
	// Application profiles map application names to the names of its profile, to be used when deploying the application
	//  as part of the deployment package together with the deployment profile.
	ApplicationProfiles CatalogV3DeploymentProfile_ApplicationProfiles `json:"applicationProfiles"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) Description of the deployment profile. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the registry. When specified, it must be unique among all
	//  profiles of a given package. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// Name is a human-readable unique identifier for the profile and must be unique for all profiles of a
	//  given deployment package.
	Name string `json:"name"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`
}

// Application profiles map application names to the names of its profile, to be used when deploying the application
//
//	as part of the deployment package together with the deployment profile.
type CatalogV3DeploymentProfile_ApplicationProfiles struct {
	AdditionalProperties map[string]string `json:"-"`
}

// DeploymentRequirement is a reference to the deployment package that must be deployed first,
//
//	as a requirement for an application to be deployed.
type CatalogV3DeploymentRequirement struct {
	// (OPTIONAL) Optional name of the deployment profile to be used. When not provided, the default deployment profile will be used.
	DeploymentProfileName *string `json:"deploymentProfileName,omitempty"`

	// Name of the required deployment package.
	Name string `json:"name"`

	// Version of the required deployment package.
	Version string `json:"version"`
}

// Endpoint represents an application service endpoint.
type CatalogV3Endpoint struct {
	// (OPTIONAL) The name of the application providing this endpoint.
	AppName *string `json:"appName,omitempty"`

	// Authentication type expected by the endpoint.
	AuthType string `json:"authType"`

	// Externally accessible path to the endpoint.
	ExternalPath string `json:"externalPath"`

	// Internally accessible path to the endpoint.
	InternalPath string `json:"internalPath"`

	// Protocol scheme provided by the endpoint.
	Scheme string `json:"scheme"`

	// The name of the service hosted by the endpoint.
	ServiceName string `json:"serviceName"`
}

// Response message for the GetApplicationReferenceCount method.
type CatalogV3GetApplicationReferenceCountResponse struct {
	ReferenceCount int `json:"referenceCount"`
}

// Response message for the GetApplication method.
type CatalogV3GetApplicationResponse struct {
	// Application represents a Helm chart that can be deployed to one or more Kubernetes pods.
	Application CatalogV3Application `json:"application"`
}

// Response message for the GetApplication method.
type CatalogV3GetApplicationVersionsResponse struct {
	// A list of applications with the same project and name. TODO rename to 'applications'
	Application []CatalogV3Application `json:"application"`
}

// Response message for the GetArtifact method.
type CatalogV3GetArtifactResponse struct {
	// Artifact represents a binary artifact that can be used for various purposes, e.g. icon or thumbnail for UI display, or
	//  auxiliary artifacts for integration with various platform services such as Grafana dashboard and similar. An artifact may be
	//  used by multiple deployment packages.
	Artifact CatalogV3Artifact `json:"artifact"`
}

// Response message for the GetDeploymentPackage method.
type CatalogV3GetDeploymentPackageResponse struct {
	// DeploymentPackage represents a collection of applications (referenced by their name and a version) that are
	//  deployed together. The package can define one or more deployment profiles that specify the individual application
	//  profiles to be used when deploying each application. If applications need to be deployed in a particular order, the
	//  package can also define any startup dependencies between its constituent applications as a set of dependency graph edges.
	//
	//  The deployment package can also refer to a set of artifacts used for miscellaneous purposes,
	//  e.g. a thumbnail, icon, or a Grafana extension.
	DeploymentPackage CatalogV3DeploymentPackage `json:"deploymentPackage"`
}

// Response message for the GetDeploymentPackageVersions method.
type CatalogV3GetDeploymentPackageVersionsResponse struct {
	// A list of DeploymentPackages with the same project and name.
	DeploymentPackages []CatalogV3DeploymentPackage `json:"deploymentPackages"`
}

// Response message for the GetRegistry method.
type CatalogV3GetRegistryResponse struct {
	// Registry represents a repository from which various artifacts, such as application Docker\* images or Helm\* charts
	//  can be retrieved. As such, the registry entity holds information used for finding and accessing the represented repository.
	Registry CatalogV3Registry `json:"registry"`
}

// Response message for the Import method
type CatalogV3ImportResponse struct {
	// Any error messages encountered either during chart parsing or entity creation or update.
	ErrorMessages *[]string `json:"errorMessages,omitempty"`
}

// Kind designation for applications and packages, normal (unspecified), extension, or addon.
type CatalogV3Kind string

// Response message for the ListApplications method.
type CatalogV3ListApplicationsResponse struct {
	// A list of applications.
	Applications []CatalogV3Application `json:"applications"`

	// Count of items in the entire list, regardless of pagination.
	TotalElements int32 `json:"totalElements"`
}

// Response message for the ListArtifacts method.
type CatalogV3ListArtifactsResponse struct {
	// A list of artifacts.
	Artifacts []CatalogV3Artifact `json:"artifacts"`

	// Count of items in the entire list, regardless of pagination.
	TotalElements int32 `json:"totalElements"`
}

// Response message for the ListDeploymentPackages method.
type CatalogV3ListDeploymentPackagesResponse struct {
	// A list of DeploymentPackages.
	DeploymentPackages []CatalogV3DeploymentPackage `json:"deploymentPackages"`

	// Count of items in the entire list, regardless of pagination.
	TotalElements int32 `json:"totalElements"`
}

// Response message for the ListRegistries method.
type CatalogV3ListRegistriesResponse struct {
	// A list of registries.
	Registries []CatalogV3Registry `json:"registries"`

	// Count of items in the entire list, regardless of pagination.
	TotalElements int32 `json:"totalElements"`
}

// Namespace represents a complex namespace definition with predefined labels and annotations.
//
//	They are created before any other resources in the deployment.
type CatalogV3Namespace struct {
	// (OPTIONAL)
	Annotations *CatalogV3Namespace_Annotations `json:"annotations,omitempty"`

	// (OPTIONAL)
	Labels *CatalogV3Namespace_Labels `json:"labels,omitempty"`

	// namespace names must be valid RFC 1123 DNS labels.
	//  Avoid creating namespaces with the prefix `kube-`, since it is reserved for Kubernetes\* system namespaces.
	//  Avoid `default` - will already exist
	Name string `json:"name"`
}

// (OPTIONAL)
type CatalogV3Namespace_Annotations struct {
	AdditionalProperties map[string]string `json:"-"`
}

// (OPTIONAL)
type CatalogV3Namespace_Labels struct {
	AdditionalProperties map[string]string `json:"-"`
}

// ParameterTemplate describes override values for Helm chart values
type CatalogV3ParameterTemplate struct {
	// (OPTIONAL) Default value for the parameter.
	Default *string `json:"default,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the template. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// (OPTIONAL) Optional mandatory flag for the parameter.
	Mandatory *bool `json:"mandatory,omitempty"`

	// Human-readable name for the parameter template.
	Name string `json:"name"`

	// (OPTIONAL) Optional secret flag for the parameter.
	Secret *bool `json:"secret,omitempty"`

	// (OPTIONAL) List of suggested values to use, to override the default value.
	SuggestedValues *[]string `json:"suggestedValues,omitempty"`

	// Type of parameter: string, number, or boolean.
	Type string `json:"type"`

	// (OPTIONAL) Optional validator for the parameter. Usage TBD.
	Validator *string `json:"validator,omitempty"`
}

// Profile is a set of configuration values for customizing application deployment.
type CatalogV3Profile struct {
	// (OPTIONAL) Raw byte value containing the chart values as raw YAML bytes.
	ChartValues *string `json:"chartValues,omitempty"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) List of deployment requirements for this profile.
	DeploymentRequirement *[]CatalogV3DeploymentRequirement `json:"deploymentRequirement,omitempty"`

	// (OPTIONAL) Description of the profile. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the profile. When specified, it must be unique among all
	//  profiles of a given application. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// Human-readable name for the profile. Unique among all profiles of the same application.
	Name string `json:"name"`

	// (OPTIONAL) Parameter templates available for this profile.
	ParameterTemplates *[]CatalogV3ParameterTemplate `json:"parameterTemplates,omitempty"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`
}

// Registry represents a repository from which various artifacts, such as application Docker\* images or Helm\* charts
//
//	can be retrieved. As such, the registry entity holds information used for finding and accessing the represented repository.
type CatalogV3Registry struct {
	// (OPTIONAL) Optional type of the API used to obtain inventory of the articles hosted by the registry.
	ApiType *string `json:"apiType,omitempty"`

	// Optional authentication token or password for accessing the registry.
	AuthToken *string `json:"authToken,omitempty"`

	// (OPTIONAL) Optional CA certificates for accessing the registry using secure channels, such as HTTPS.
	Cacerts *string `json:"cacerts,omitempty"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	CreateTime *GoogleProtobufTimestamp `json:"createTime,omitempty"`

	// (OPTIONAL) Description of the registry. Displayed on user interfaces.
	Description *string `json:"description,omitempty"`

	// (OPTIONAL) Display name is an optional human-readable name for the registry. When specified, it must be unique among all
	//  registries within a project. It is used for display purposes on user interfaces.
	DisplayName *string `json:"displayName,omitempty"`

	// (OPTIONAL) Optional URL of the API for accessing inventory of artifacts hosted by the registry.
	InventoryUrl *string `json:"inventoryUrl,omitempty"`

	// Name is a human-readable unique identifier for the registry and must be unique for all registries of a
	//  given project.
	Name string `json:"name"`

	// Root URL for retrieving artifacts, e.g. Docker images and Helm charts, from the registry.
	RootUrl string `json:"rootUrl"`

	// Type indicates whether the registry holds Docker images or Helm charts; defaults to Helm charts.
	Type string `json:"type"`

	// A Timestamp represents a point in time independent of any time zone or local
	//  calendar, encoded as a count of seconds and fractions of seconds at
	//  nanosecond resolution. The count is relative to an epoch at UTC midnight on
	//  January 1, 1970, in the proleptic Gregorian calendar which extends the
	//  Gregorian calendar backwards to year one.
	//
	//  All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
	//  second table is needed for interpretation, using a [24-hour linear
	//  smear](https://developers.google.com/time/smear).
	//
	//  The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
	//  restricting to that range, we ensure that we can convert to and from [RFC
	//  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
	//
	//  # Examples
	//
	//  Example 1: Compute Timestamp from POSIX `time()`.
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(time(NULL));
	//      timestamp.set_nanos(0);
	//
	//  Example 2: Compute Timestamp from POSIX `gettimeofday()`.
	//
	//      struct timeval tv;
	//      gettimeofday(&tv, NULL);
	//
	//      Timestamp timestamp;
	//      timestamp.set_seconds(tv.tv_sec);
	//      timestamp.set_nanos(tv.tv_usec * 1000);
	//
	//  Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
	//
	//      FILETIME ft;
	//      GetSystemTimeAsFileTime(&ft);
	//      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
	//
	//      // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
	//      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
	//      Timestamp timestamp;
	//      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
	//      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
	//
	//  Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
	//
	//      long millis = System.currentTimeMillis();
	//
	//      Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
	//          .setNanos((int) ((millis % 1000) * 1000000)).build();
	//
	//  Example 5: Compute Timestamp from Java `Instant.now()`.
	//
	//      Instant now = Instant.now();
	//
	//      Timestamp timestamp =
	//          Timestamp.newBuilder().setSeconds(now.getEpochSecond())
	//              .setNanos(now.getNano()).build();
	//
	//  Example 6: Compute Timestamp from current time in Python.
	//
	//      timestamp = Timestamp()
	//      timestamp.GetCurrentTime()
	//
	//  # JSON Mapping
	//
	//  In JSON format, the Timestamp type is encoded as a string in the
	//  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
	//  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
	//  where {year} is always expressed using four digits while {month}, {day},
	//  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
	//  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
	//  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
	//  is required. A proto3 JSON serializer should always use UTC (as indicated by
	//  "Z") when printing the Timestamp type and a proto3 JSON parser should be
	//  able to accept both UTC and other timezones (as indicated by an offset).
	//
	//  For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
	//  01:30 UTC on January 15, 2017.
	//
	//  In JavaScript, one can convert a Date object to this format using the
	//  standard
	//  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
	//  method. In Python, a standard `datetime.datetime` object can be converted
	//  to this format using
	//  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
	//  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
	//  the Joda Time's [`ISODateTimeFormat.dateTime()`](
	//  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
	//  ) to obtain a formatter capable of generating timestamps in this format.
	UpdateTime *GoogleProtobufTimestamp `json:"updateTime,omitempty"`

	// Optional username for accessing the registry.
	Username *string `json:"username,omitempty"`
}

// ResourceReference represents a Kubernetes resource identifier.
type CatalogV3ResourceReference struct {
	// (OPTIONAL) Ignore whole resource if true. Will use "remove" if false or not present.
	Ignore *bool `json:"ignore,omitempty"`

	// Kubernetes resource kind, e.g. ConfigMap.
	Kind string `json:"kind"`

	// Kubernetes resource name.
	Name string `json:"name"`

	// (OPTIONAL) Kubernetes namespace where the ignored resource resides. When empty, the application namespace will be used.
	Namespace *string `json:"namespace,omitempty"`
}

// UIExtension is an augmentation of an API extension.
type CatalogV3UIExtension struct {
	// The name of the application corresponding to this UI extension.
	AppName string `json:"appName"`

	// Description of the API extension, used on the main UI dashboard.
	Description string `json:"description"`

	// The name of the main file to load this specific UI extension.
	FileName string `json:"fileName"`

	// Label is a human readable text used for display in the main UI dashboard
	Label string `json:"label"`

	// Name of the application module to be loaded.
	ModuleName string `json:"moduleName"`

	// The name of the API extension endpoint.
	ServiceName string `json:"serviceName"`
}

// Upload represents a single file-upload record.
type CatalogV3Upload struct {
	// Raw bytes content of the file being uploaded.
	Artifact []byte `json:"artifact"`

	// Name of the file being uploaded.
	FileName string `json:"fileName"`
}

// Response message for the UploadCatalogItems method
type CatalogV3UploadCatalogEntitiesResponse struct {
	// Any error messages encountered either during YAML parsing or entity creation or update.
	ErrorMessages *[]string `json:"errorMessages,omitempty"`

	// Session ID, generated by the server after the first upload request has been processed.
	SessionId string `json:"sessionId"`

	// Deprecated: Next expected upload number or total number of uploads on the last upload request.
	UploadNumber int `json:"uploadNumber"`
}

// A generic empty message that you can re-use to avoid defining duplicated
//
//	empty messages in your APIs. A typical example is to use it as the request
//	or the response type of an API method. For instance:
//
//	    service Foo {
//	      rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty);
//	    }
type GoogleProtobufEmpty = map[string]interface{}

// A Timestamp represents a point in time independent of any time zone or local
//
//	calendar, encoded as a count of seconds and fractions of seconds at
//	nanosecond resolution. The count is relative to an epoch at UTC midnight on
//	January 1, 1970, in the proleptic Gregorian calendar which extends the
//	Gregorian calendar backwards to year one.
//
//	All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
//	second table is needed for interpretation, using a [24-hour linear
//	smear](https://developers.google.com/time/smear).
//
//	The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
//	restricting to that range, we ensure that we can convert to and from [RFC
//	3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.
//
//	# Examples
//
//	Example 1: Compute Timestamp from POSIX `time()`.
//
//	    Timestamp timestamp;
//	    timestamp.set_seconds(time(NULL));
//	    timestamp.set_nanos(0);
//
//	Example 2: Compute Timestamp from POSIX `gettimeofday()`.
//
//	    struct timeval tv;
//	    gettimeofday(&tv, NULL);
//
//	    Timestamp timestamp;
//	    timestamp.set_seconds(tv.tv_sec);
//	    timestamp.set_nanos(tv.tv_usec * 1000);
//
//	Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.
//
//	    FILETIME ft;
//	    GetSystemTimeAsFileTime(&ft);
//	    UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;
//
//	    // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
//	    // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
//	    Timestamp timestamp;
//	    timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
//	    timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));
//
//	Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.
//
//	    long millis = System.currentTimeMillis();
//
//	    Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
//	        .setNanos((int) ((millis % 1000) * 1000000)).build();
//
//	Example 5: Compute Timestamp from Java `Instant.now()`.
//
//	    Instant now = Instant.now();
//
//	    Timestamp timestamp =
//	        Timestamp.newBuilder().setSeconds(now.getEpochSecond())
//	            .setNanos(now.getNano()).build();
//
//	Example 6: Compute Timestamp from current time in Python.
//
//	    timestamp = Timestamp()
//	    timestamp.GetCurrentTime()
//
//	# JSON Mapping
//
//	In JSON format, the Timestamp type is encoded as a string in the
//	[RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
//	format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
//	where {year} is always expressed using four digits while {month}, {day},
//	{hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
//	seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
//	are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
//	is required. A proto3 JSON serializer should always use UTC (as indicated by
//	"Z") when printing the Timestamp type and a proto3 JSON parser should be
//	able to accept both UTC and other timezones (as indicated by an offset).
//
//	For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
//	01:30 UTC on January 15, 2017.
//
//	In JavaScript, one can convert a Date object to this format using the
//	standard
//	[toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
//	method. In Python, a standard `datetime.datetime` object can be converted
//	to this format using
//	[`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
//	the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
//	the Joda Time's [`ISODateTimeFormat.dateTime()`](
//	http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
//	) to obtain a formatter capable of generating timestamps in this format.
type GoogleProtobufTimestamp = time.Time

// CatalogServiceListApplicationsParams defines parameters for CatalogServiceListApplications.
type CatalogServiceListApplicationsParams struct {
	// Names the field to be used for ordering the returned results.
	OrderBy *string `form:"orderBy,omitempty" json:"orderBy,omitempty"`

	// Expression to use for filtering the results.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`

	// Maximum number of items to return.
	PageSize *int32 `form:"pageSize,omitempty" json:"pageSize,omitempty"`

	// Index of the first item to return.
	Offset *int32 `form:"offset,omitempty" json:"offset,omitempty"`

	// List of application kinds to be returned; empty list means all kinds.
	Kinds *[]CatalogV3Kind `form:"kinds,omitempty" json:"kinds,omitempty"`
}

// CatalogServiceCreateApplicationJSONBody defines parameters for CatalogServiceCreateApplication.
type CatalogServiceCreateApplicationJSONBody = CatalogV3Application

// CatalogServiceUpdateApplicationJSONBody defines parameters for CatalogServiceUpdateApplication.
type CatalogServiceUpdateApplicationJSONBody = CatalogV3Application

// CatalogServiceListArtifactsParams defines parameters for CatalogServiceListArtifacts.
type CatalogServiceListArtifactsParams struct {
	// Names the field to be used for ordering the returned results.
	OrderBy *string `form:"orderBy,omitempty" json:"orderBy,omitempty"`

	// Expression to use for filtering the results.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`

	// Maximum number of items to return.
	PageSize *int32 `form:"pageSize,omitempty" json:"pageSize,omitempty"`

	// Index of the first item to return.
	Offset *int32 `form:"offset,omitempty" json:"offset,omitempty"`
}

// CatalogServiceCreateArtifactJSONBody defines parameters for CatalogServiceCreateArtifact.
type CatalogServiceCreateArtifactJSONBody = CatalogV3Artifact

// CatalogServiceUpdateArtifactJSONBody defines parameters for CatalogServiceUpdateArtifact.
type CatalogServiceUpdateArtifactJSONBody = CatalogV3Artifact

// CatalogServiceListDeploymentPackagesParams defines parameters for CatalogServiceListDeploymentPackages.
type CatalogServiceListDeploymentPackagesParams struct {
	// Names the field to be used for ordering the returned results.
	OrderBy *string `form:"orderBy,omitempty" json:"orderBy,omitempty"`

	// Expression to use for filtering the results.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`

	// Maximum number of items to return.
	PageSize *int32 `form:"pageSize,omitempty" json:"pageSize,omitempty"`

	// Index of the first item to return.
	Offset *int32 `form:"offset,omitempty" json:"offset,omitempty"`

	// List of deployment package kinds to be returned; empty list means all kinds.
	Kinds *[]CatalogV3Kind `form:"kinds,omitempty" json:"kinds,omitempty"`
}

// CatalogServiceCreateDeploymentPackageJSONBody defines parameters for CatalogServiceCreateDeploymentPackage.
type CatalogServiceCreateDeploymentPackageJSONBody = CatalogV3DeploymentPackage

// CatalogServiceUpdateDeploymentPackageJSONBody defines parameters for CatalogServiceUpdateDeploymentPackage.
type CatalogServiceUpdateDeploymentPackageJSONBody = CatalogV3DeploymentPackage

// CatalogServiceImportParams defines parameters for CatalogServiceImport.
type CatalogServiceImportParams struct {
	// Required URL of Helm Chart to import
	Url string `form:"url" json:"url"`

	// Optional username for downloading from the URL
	Username *string `form:"username,omitempty" json:"username,omitempty"`

	// Optional authentication token or password for downloading from the URL
	AuthToken *string `form:"authToken,omitempty" json:"authToken,omitempty"`

	// Optional raw byte value containing the chart values as raw YAML bytes.
	ChartValues *string `form:"chartValues,omitempty" json:"chartValues,omitempty"`

	// If true and a username/auth_token is specified then they will be included
	//  in the generated Registry object.
	IncludeAuth *bool `form:"includeAuth,omitempty" json:"includeAuth,omitempty"`

	// If true and chart_values is not set, then the values.yaml will be extracted and
	//  used to generate default profile values.
	GenerateDefaultValues *bool `form:"generateDefaultValues,omitempty" json:"generateDefaultValues,omitempty"`

	// Generates default parameters from the values, from chart_values or from
	//  generate_default_values as appropriate.
	GenerateDefaultParameters *bool `form:"generateDefaultParameters,omitempty" json:"generateDefaultParameters,omitempty"`

	// Optional namespace
	Namespace *string `form:"namespace,omitempty" json:"namespace,omitempty"`
}

// CatalogServiceListRegistriesParams defines parameters for CatalogServiceListRegistries.
type CatalogServiceListRegistriesParams struct {
	// Names the field to be used for ordering the returned results.
	OrderBy *string `form:"orderBy,omitempty" json:"orderBy,omitempty"`

	// Expression to use for filtering the results.
	Filter *string `form:"filter,omitempty" json:"filter,omitempty"`

	// Maximum number of items to return.
	PageSize *int32 `form:"pageSize,omitempty" json:"pageSize,omitempty"`

	// Index of the first item to return.
	Offset *int32 `form:"offset,omitempty" json:"offset,omitempty"`

	// Request that sensitive information, such as username, auth_token, and CA certificates are included in the response.
	ShowSensitiveInfo *bool `form:"showSensitiveInfo,omitempty" json:"showSensitiveInfo,omitempty"`
}

// CatalogServiceCreateRegistryJSONBody defines parameters for CatalogServiceCreateRegistry.
type CatalogServiceCreateRegistryJSONBody = CatalogV3Registry

// CatalogServiceGetRegistryParams defines parameters for CatalogServiceGetRegistry.
type CatalogServiceGetRegistryParams struct {
	// Request that sensitive information, such as username, auth_token, and CA certificates are included in the response.
	ShowSensitiveInfo *bool `form:"showSensitiveInfo,omitempty" json:"showSensitiveInfo,omitempty"`
}

// CatalogServiceUpdateRegistryJSONBody defines parameters for CatalogServiceUpdateRegistry.
type CatalogServiceUpdateRegistryJSONBody = CatalogV3Registry

// CatalogServiceUploadCatalogEntitiesJSONBody defines parameters for CatalogServiceUploadCatalogEntities.
type CatalogServiceUploadCatalogEntitiesJSONBody = CatalogV3Upload

// CatalogServiceUploadCatalogEntitiesParams defines parameters for CatalogServiceUploadCatalogEntities.
type CatalogServiceUploadCatalogEntitiesParams struct {
	// First upload request in the batch must not specify session ID. Subsequent upload requests must copy
	//  the session ID from the previously issued response.
	SessionId *string `form:"sessionId,omitempty" json:"sessionId,omitempty"`

	// Deprecated: Upload number must increase sequentially, starting with 1.
	UploadNumber *int `form:"uploadNumber,omitempty" json:"uploadNumber,omitempty"`

	// Must be set to 'true' to perform load of all entity files uploaded as part of this session.
	LastUpload *bool `form:"lastUpload,omitempty" json:"lastUpload,omitempty"`
}

// CatalogServiceCreateApplicationJSONRequestBody defines body for CatalogServiceCreateApplication for application/json ContentType.
type CatalogServiceCreateApplicationJSONRequestBody = CatalogServiceCreateApplicationJSONBody

// CatalogServiceUpdateApplicationJSONRequestBody defines body for CatalogServiceUpdateApplication for application/json ContentType.
type CatalogServiceUpdateApplicationJSONRequestBody = CatalogServiceUpdateApplicationJSONBody

// CatalogServiceCreateArtifactJSONRequestBody defines body for CatalogServiceCreateArtifact for application/json ContentType.
type CatalogServiceCreateArtifactJSONRequestBody = CatalogServiceCreateArtifactJSONBody

// CatalogServiceUpdateArtifactJSONRequestBody defines body for CatalogServiceUpdateArtifact for application/json ContentType.
type CatalogServiceUpdateArtifactJSONRequestBody = CatalogServiceUpdateArtifactJSONBody

// CatalogServiceCreateDeploymentPackageJSONRequestBody defines body for CatalogServiceCreateDeploymentPackage for application/json ContentType.
type CatalogServiceCreateDeploymentPackageJSONRequestBody = CatalogServiceCreateDeploymentPackageJSONBody

// CatalogServiceUpdateDeploymentPackageJSONRequestBody defines body for CatalogServiceUpdateDeploymentPackage for application/json ContentType.
type CatalogServiceUpdateDeploymentPackageJSONRequestBody = CatalogServiceUpdateDeploymentPackageJSONBody

// CatalogServiceCreateRegistryJSONRequestBody defines body for CatalogServiceCreateRegistry for application/json ContentType.
type CatalogServiceCreateRegistryJSONRequestBody = CatalogServiceCreateRegistryJSONBody

// CatalogServiceUpdateRegistryJSONRequestBody defines body for CatalogServiceUpdateRegistry for application/json ContentType.
type CatalogServiceUpdateRegistryJSONRequestBody = CatalogServiceUpdateRegistryJSONBody

// CatalogServiceUploadCatalogEntitiesJSONRequestBody defines body for CatalogServiceUploadCatalogEntities for application/json ContentType.
type CatalogServiceUploadCatalogEntitiesJSONRequestBody = CatalogServiceUploadCatalogEntitiesJSONBody

// Getter for additional properties for CatalogV3DeploymentPackage_DefaultNamespaces. Returns the specified
// element and whether it was found
func (a CatalogV3DeploymentPackage_DefaultNamespaces) Get(fieldName string) (value string, found bool) {
	if a.AdditionalProperties != nil {
		value, found = a.AdditionalProperties[fieldName]
	}
	return
}

// Setter for additional properties for CatalogV3DeploymentPackage_DefaultNamespaces
func (a *CatalogV3DeploymentPackage_DefaultNamespaces) Set(fieldName string, value string) {
	if a.AdditionalProperties == nil {
		a.AdditionalProperties = make(map[string]string)
	}
	a.AdditionalProperties[fieldName] = value
}

// Override default JSON handling for CatalogV3DeploymentPackage_DefaultNamespaces to handle AdditionalProperties
func (a *CatalogV3DeploymentPackage_DefaultNamespaces) UnmarshalJSON(b []byte) error {
	object := make(map[string]json.RawMessage)
	err := json.Unmarshal(b, &object)
	if err != nil {
		return err
	}

	if len(object) != 0 {
		a.AdditionalProperties = make(map[string]string)
		for fieldName, fieldBuf := range object {
			var fieldVal string
			err := json.Unmarshal(fieldBuf, &fieldVal)
			if err != nil {
				return fmt.Errorf("error unmarshaling field %s: %w", fieldName, err)
			}
			a.AdditionalProperties[fieldName] = fieldVal
		}
	}
	return nil
}

// Override default JSON handling for CatalogV3DeploymentPackage_DefaultNamespaces to handle AdditionalProperties
func (a CatalogV3DeploymentPackage_DefaultNamespaces) MarshalJSON() ([]byte, error) {
	var err error
	object := make(map[string]json.RawMessage)

	for fieldName, field := range a.AdditionalProperties {
		object[fieldName], err = json.Marshal(field)
		if err != nil {
			return nil, fmt.Errorf("error marshaling '%s': %w", fieldName, err)
		}
	}
	return json.Marshal(object)
}

// Getter for additional properties for CatalogV3DeploymentProfile_ApplicationProfiles. Returns the specified
// element and whether it was found
func (a CatalogV3DeploymentProfile_ApplicationProfiles) Get(fieldName string) (value string, found bool) {
	if a.AdditionalProperties != nil {
		value, found = a.AdditionalProperties[fieldName]
	}
	return
}

// Setter for additional properties for CatalogV3DeploymentProfile_ApplicationProfiles
func (a *CatalogV3DeploymentProfile_ApplicationProfiles) Set(fieldName string, value string) {
	if a.AdditionalProperties == nil {
		a.AdditionalProperties = make(map[string]string)
	}
	a.AdditionalProperties[fieldName] = value
}

// Override default JSON handling for CatalogV3DeploymentProfile_ApplicationProfiles to handle AdditionalProperties
func (a *CatalogV3DeploymentProfile_ApplicationProfiles) UnmarshalJSON(b []byte) error {
	object := make(map[string]json.RawMessage)
	err := json.Unmarshal(b, &object)
	if err != nil {
		return err
	}

	if len(object) != 0 {
		a.AdditionalProperties = make(map[string]string)
		for fieldName, fieldBuf := range object {
			var fieldVal string
			err := json.Unmarshal(fieldBuf, &fieldVal)
			if err != nil {
				return fmt.Errorf("error unmarshaling field %s: %w", fieldName, err)
			}
			a.AdditionalProperties[fieldName] = fieldVal
		}
	}
	return nil
}

// Override default JSON handling for CatalogV3DeploymentProfile_ApplicationProfiles to handle AdditionalProperties
func (a CatalogV3DeploymentProfile_ApplicationProfiles) MarshalJSON() ([]byte, error) {
	var err error
	object := make(map[string]json.RawMessage)

	for fieldName, field := range a.AdditionalProperties {
		object[fieldName], err = json.Marshal(field)
		if err != nil {
			return nil, fmt.Errorf("error marshaling '%s': %w", fieldName, err)
		}
	}
	return json.Marshal(object)
}

// Getter for additional properties for CatalogV3Namespace_Annotations. Returns the specified
// element and whether it was found
func (a CatalogV3Namespace_Annotations) Get(fieldName string) (value string, found bool) {
	if a.AdditionalProperties != nil {
		value, found = a.AdditionalProperties[fieldName]
	}
	return
}

// Setter for additional properties for CatalogV3Namespace_Annotations
func (a *CatalogV3Namespace_Annotations) Set(fieldName string, value string) {
	if a.AdditionalProperties == nil {
		a.AdditionalProperties = make(map[string]string)
	}
	a.AdditionalProperties[fieldName] = value
}

// Override default JSON handling for CatalogV3Namespace_Annotations to handle AdditionalProperties
func (a *CatalogV3Namespace_Annotations) UnmarshalJSON(b []byte) error {
	object := make(map[string]json.RawMessage)
	err := json.Unmarshal(b, &object)
	if err != nil {
		return err
	}

	if len(object) != 0 {
		a.AdditionalProperties = make(map[string]string)
		for fieldName, fieldBuf := range object {
			var fieldVal string
			err := json.Unmarshal(fieldBuf, &fieldVal)
			if err != nil {
				return fmt.Errorf("error unmarshaling field %s: %w", fieldName, err)
			}
			a.AdditionalProperties[fieldName] = fieldVal
		}
	}
	return nil
}

// Override default JSON handling for CatalogV3Namespace_Annotations to handle AdditionalProperties
func (a CatalogV3Namespace_Annotations) MarshalJSON() ([]byte, error) {
	var err error
	object := make(map[string]json.RawMessage)

	for fieldName, field := range a.AdditionalProperties {
		object[fieldName], err = json.Marshal(field)
		if err != nil {
			return nil, fmt.Errorf("error marshaling '%s': %w", fieldName, err)
		}
	}
	return json.Marshal(object)
}

// Getter for additional properties for CatalogV3Namespace_Labels. Returns the specified
// element and whether it was found
func (a CatalogV3Namespace_Labels) Get(fieldName string) (value string, found bool) {
	if a.AdditionalProperties != nil {
		value, found = a.AdditionalProperties[fieldName]
	}
	return
}

// Setter for additional properties for CatalogV3Namespace_Labels
func (a *CatalogV3Namespace_Labels) Set(fieldName string, value string) {
	if a.AdditionalProperties == nil {
		a.AdditionalProperties = make(map[string]string)
	}
	a.AdditionalProperties[fieldName] = value
}

// Override default JSON handling for CatalogV3Namespace_Labels to handle AdditionalProperties
func (a *CatalogV3Namespace_Labels) UnmarshalJSON(b []byte) error {
	object := make(map[string]json.RawMessage)
	err := json.Unmarshal(b, &object)
	if err != nil {
		return err
	}

	if len(object) != 0 {
		a.AdditionalProperties = make(map[string]string)
		for fieldName, fieldBuf := range object {
			var fieldVal string
			err := json.Unmarshal(fieldBuf, &fieldVal)
			if err != nil {
				return fmt.Errorf("error unmarshaling field %s: %w", fieldName, err)
			}
			a.AdditionalProperties[fieldName] = fieldVal
		}
	}
	return nil
}

// Override default JSON handling for CatalogV3Namespace_Labels to handle AdditionalProperties
func (a CatalogV3Namespace_Labels) MarshalJSON() ([]byte, error) {
	var err error
	object := make(map[string]json.RawMessage)

	for fieldName, field := range a.AdditionalProperties {
		object[fieldName], err = json.Marshal(field)
		if err != nil {
			return nil, fmt.Errorf("error marshaling '%s': %w", fieldName, err)
		}
	}
	return json.Marshal(object)
}
